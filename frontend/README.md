# SkinSupport AI Community (Frontend)

Frontend for the MERN project **SkinSupport AI Community**, built with:

- React
- Vite
- JavaScript (no TypeScript)
- Tailwind CSS

## Run locally

```bash
npm install
npm run dev
```

Frontend runs at `http://localhost:5173`.

## Build

```bash
npm run build
```
